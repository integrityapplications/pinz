Somes simple hack code to learn node.js server sent events (SSE) and leaflet.js map API.
node-sse.js creates an SSE event every 5 seconds containing a number.
myFirstMap.html client listens to the event source, and adds a new marker to the leaflet map, using the number received from the server.

